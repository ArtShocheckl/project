#include"clc24.ru.h"
bool security1(std::string strznak){
for(long long i; strznak[i] != '/0'; i++){
    if(strznak[i] != '+' or strznak[i] != '-' or strznak[i] != '*' ){
        bool znak = false;
        return znak;
    }
}
}
